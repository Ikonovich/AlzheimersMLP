# Tests of individual layers including Linear, Convolutional, and Recurrent.
# Layers are tested against outputs of known good layer models using deterministic processes
# Specifically, each model is tested against the equivalent PyTorch layer.



